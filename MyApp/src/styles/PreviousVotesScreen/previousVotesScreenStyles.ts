import { StyleSheet } from "react-native"

const previousVotesScreenStyles = StyleSheet.create({
    previousVotesButtonContainer:{
        flexDirection: "row",
        flexWrap: "wrap",
        justifyContent:"space-between"
    }
})

export default previousVotesScreenStyles;